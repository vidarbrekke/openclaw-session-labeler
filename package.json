{
  "name": "@openclaw/session-labeler",
  "version": "0.1.10",
  "description": "Gives sessions a friendly name — auto-labels OpenClaw sessions after 3 user requests",
  "type": "module",
  "license": "MIT",
  "files": [
    "hooks/",
    "src/",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "openclaw": {
    "hooks": [
      "./hooks/session-labeler"
    ]
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "validate:hook": "node scripts/validate-hook-pack.mjs",
    "validate:pack": "node scripts/validate-packaging.mjs",
    "validate": "npm run validate:hook && npm run typecheck && npm test && npm run validate:pack",
    "dist": "./create_dist.sh"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}
